<ul class="o-auto-grid">
    {% for item in items %}
    <li>
        {% render '@card', { image:item.image, title:item.title, content:item.content, button:item.button } %}
    </li>
    {% endfor %}
</ul>
