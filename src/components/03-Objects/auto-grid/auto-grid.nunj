<ul class="o-auto-grid">
    {% for item in items %}
    <li>
        {% if item.title %}<h2 class="u-h2">{{ item.title }}</h2>{% endif %}
        {% if item.content %}<p>{{ item.content }}</p>{% endif %}
    </li>
    {% endfor %}
</ul>
