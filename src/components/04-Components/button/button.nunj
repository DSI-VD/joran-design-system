<a href="{{ url }}"
   class="c-button
   {%- if variantModifier %} {{ variantModifier }}{% endif %}
   {%- if styleModifier %} {{ styleModifier }}{% endif -%}">
    {%- if svg -%}<span>{%- endif -%}
    {{ text }}
    {%- if svg -%}</span>{% render '@svg-chevron-right' %}{%- endif -%}
</a>