<figure class="c-figure">
    <img class="c-figure__media" src="{{ src }}" alt="{{ alt }}">
    {% if caption -%}
    <figcaption class="c-figure__caption">
        {{ caption }}
    </figcaption>
    {%- endif %}
</figure>