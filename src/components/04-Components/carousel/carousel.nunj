<div class="c-carousel main-carousel" data-flickity>
    {% for article in articles %}
    <div class="c-carousel__cell aspect-w-16 aspect-h-9 carousel-cell">
        {% render '@figure', article.image, true %}
    </div>
    {% endfor %}
</div>